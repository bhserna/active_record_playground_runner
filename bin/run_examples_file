#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "active_record_examples_runner"

path = ARGV[0]
code = File.read(path)
runner = ActiveRecordExamplesRunner::FileRunner.new(path) { eval(code) }
runner.setup.run.destroy