# frozen_string_literal: true

RSpec.describe ActiveRecordExamplesRunner do
  it "has a version number" do
    expect(ActiveRecordExamplesRunner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
